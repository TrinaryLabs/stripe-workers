//import { expect } from 'chai'
//import { stripe } from '../../_setup'

// Can't test => Error: Only live keys can access this method.

/*
describe('Sigma', async () => {
    describe('ScheduledQueryRun Resource', async () => {
        describe('retrieve', async () => {
            it('Sends the correct request', async () => {
                const response = await stripe.sigma.scheduledQueryRuns.retrieve('sqr_1IhXAC2eZvKYlo2CbEkxjeT4')

                expect(response.id).to.equal('sqr_1IhXAC2eZvKYlo2CbEkxjeT4')
                expect(response.object).to.equal('scheduled_query_run')
            })
        })

        describe('list', async () => {
            it('Sends the correct request', async () => {
                const response = await stripe.sigma.scheduledQueryRuns.list()

                expect(response.object).to.equal('list')
                expect(response.url).to.equal('/v1/sigma/scheduled_query_runs')
            })
        })
    })
})
*/
